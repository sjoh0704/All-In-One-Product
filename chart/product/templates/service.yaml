apiVersion: v1
kind: Service
metadata:
  name:  product
spec:
  selector:
    app: product
  ports:
    - name: http
      port: 8080
      targetPort: 8000
  type: ClusterIP

---

apiVersion: v1
kind: Service
metadata:
  name: product-db
spec:
  ports:
  - port: 3306
    targetPort: 3306
  selector:
    app: product-db
